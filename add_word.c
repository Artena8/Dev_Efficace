#include <stdlib.h>
#include levenshtein.c

int main(int argc, char * arv[]) {
    /*
        args = 
            gameparty = fichier de partie
            dictionnary = dictionnaire.bin
            word = mot à ajouter
    */
    if (argc != 3) {
        printf("Usage: <word1> <word2> \n");
        return 1; // Code d'erreur
    }


    return 0;
}